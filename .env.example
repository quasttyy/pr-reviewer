# Переменные окружения для сервиса PR Reviewer
# Скопируйте этот файл в .env и измените значения при необходимости

# Окружение (dev/prod)
APP_ENV=dev

# Сервер
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# База данных
DSN=postgres://postgres:postgres@db:5432/pr_reviewer?sslmode=disable
DB_MAX_CONNS=10
DB_MIN_CONNS=2
